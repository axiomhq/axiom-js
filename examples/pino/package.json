{
  "name": "examples-pino",
  "version": "1.0.0",
  "private": true,
  "description": "examples on using axiom with pino",
  "scripts": {
    "build": "tsc",
    "format": "eslint ./src --ext .ts --quiet --fix",
    "lint": "eslint ./src --ext .ts",
    "example": "ts-node"
  },
  "dependencies": {
    "@axiomhq/pino": "workspace:*",
    "pino": "^8.14.1"
  }
}
